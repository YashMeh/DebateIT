<% posts.forEach(function(post){ %>
	<h1><%= post.title %></h1>
	<p> <%= post.body.substring(0,10) %>..</p>
	<h2><%= post.date.toString().substring(0,10) %></h2>
	<button><a href="/posts/<%= post._id%>">Read More</a></button>
	<a href="/posts/<%= post._id %>/comment/new">Add Comment</a>
<% }); %>
